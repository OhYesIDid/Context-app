node_modules/
.expo/
dist/
web-build/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Environment — never commit real keys
.env
.env.local

# Google services — contains API keys, must never be committed
google-services.json
android/google-services.json

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
